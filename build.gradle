plugins {
  id "org.openbakery.xcode-plugin" version "0.11.4"
}

xcodebuild {
  scheme = 'AppGradle'
  target = 'AppGradle'
  version = '7.0'
}

task wrapper( type: Wrapper ) {
  gradleVersion = '2.4'
}